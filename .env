# .env file (place in root directory)

# FastAPI Server URL (used by Streamlit)
API_URL=https://api-js79.onrender.com

# MLflow tracking URI (if using MLflow)
MLFLOW_TRACKING_URI=http://localhost:5000

# Optional: Model path for local loading fallback
MODEL_PATH=models/xgboost_model.json
